<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:showWhenLocked="true"
        android:turnScreenOn="true">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Full-screen alarm activity shown over the lock screen.
             Uses AppTheme.NoActionBar, NOT AppTheme.NoActionBarLaunch: the Launch theme
             extends Theme.SplashScreen with android:background=@drawable/splash (the app
             launch logo), which is only meant for MainActivity's initial frame. Using it here
             made the splash logo bleed through/overlap the alarm screen's own programmatic
             content. NoActionBar has no action bar and no background image, so the activity's
             own dark navy background (set in renderAlarmUi()) renders cleanly. -->
        <activity
            android:name=".AlarmActivity"
            android:exported="false"
            android:launchMode="singleInstance"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@style/AppTheme.NoActionBar" />

        <!-- Foreground service: looping alarm sound + vibration -->
        <service
            android:name=".AlarmService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- BroadcastReceiver: fired by AlarmManager at reminder time -->
        <receiver
            android:name=".AlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.rafiq.mobile.FIRE_ALARM" />
            </intent-filter>
        </receiver>

        <!-- BroadcastReceiver: handles Taken/I Attended/Snooze from the outer system
             notification and from AlarmActivity's buttons, entirely natively — never
             opens MainActivity. Explicit-component only, no intent-filter needed. -->
        <receiver
            android:name=".AlarmActionReceiver"
            android:exported="false" />

        <!-- BroadcastReceiver: re-schedules alarms after device reboot -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter android:priority="1">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>


        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Core -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <!-- Alarm / Notification -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Lets the app request battery-optimization exemption directly via an intent
         (ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS) so AlarmManager alarms are
         not cancelled by OEM battery managers when the app is closed. Without this
         permission the intent is silently rejected on Android 6+. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Exact alarms: USE_EXACT_ALARM (API 33+, no user grant needed) -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- Fallback for API 31-32: requires user to grant from Settings -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- Full-screen overlay for lock screen alarm UI -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Required for foreground service (AlarmService) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

</manifest>
